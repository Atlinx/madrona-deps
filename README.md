# madrona-deps

This is a fork of madrona-deps that adds support for building `aarch64` version of `madrona_mjx`. This assumes `embree` and `DirectXShaderCompiler` are manually installed on your linux machine.